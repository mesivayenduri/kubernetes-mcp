[project]
name = "kubernetes-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sai Siva Krishna Yenduri", email = "me.sivayenduri@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "kubernetes>=36.0.3",
    "mcp[cli]>=2.1.1",
]

[project.scripts]
kubernetes-mcp = "kubernetes_mcp:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
